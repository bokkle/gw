import HeartButton from '../ui/HeartButton';

const CTA = () => {
  return (
    <section className="mt-12 flex w-full items-center justify-center p-2">
      <HeartButton>Buy Now</HeartButton>
    </section>
  );
};

export default CTA;
